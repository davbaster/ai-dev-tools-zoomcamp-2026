services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: agent_relay
      POSTGRES_USER: relay
      POSTGRES_PASSWORD: relay_password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U relay -d agent_relay"]
      interval: 5s
      timeout: 5s
      retries: 10

  agent-relay:
    build:
      context: .
    image: agent-relay:local
    environment:
      RELAY_DATABASE_URL: postgresql+psycopg://relay:relay_password@postgres:5432/agent_relay
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "8001:8000"
    healthcheck:
      test:
        ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/ready')"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
